# Bootstrap LifeOS Repository

Read `AGENTS.md` and all files under `docs/`.

Then:

1. Verify the repository skeleton.
2. Implement only Phase 0 tasks under `tasks/ready/`.
3. Do not implement the full system.
4. Run every task's validation.
5. Record missing work as backlog tasks.
6. Keep decisions in repository files rather than chat history.
