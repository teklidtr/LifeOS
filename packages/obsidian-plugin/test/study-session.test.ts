import assert from "node:assert/strict";import test from "node:test";import{BridgeClient,HandshakeResult,LifeOSSettings,StudySessionController}from"../src/index.js";
class Client implements BridgeClient{calls:string[]=[];async start(_s:LifeOSSettings):Promise<HandshakeResult>{throw Error();}async call<T>(m:string):Promise<T>{this.calls.push(m);return {session_id:"s",card_paths:["flashcards/c.md"]} as T;}onNotification():()=>void{return()=>{};}async stop():Promise<void>{}}
test("study session controls stay thin",async()=>{const client=new Client();const c=new StudySessionController(client);const s=await c.start("2026-07-16",20);await c.transition(s.session_id,"finish",18);assert.deepEqual(client.calls,["study.session.start","study.session.transition"]);assert.equal(c.sourceLink(s,0),"flashcards/c.md");});
